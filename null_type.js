// Null Type 
// null represents a Variable whose value is undefined .
// Much like void , its not extrenaly useful on its onitsown.
// The null exepts only one value which is null itself, the null keyword is used 
// to define the only one value that is null itself..
var null_leteral;
// a=10 //this will not take any value.
//we cant assign any value rether then null in the null null_letteral.
// a="nikhil"
// a=3.33
// but the value null is acceptable by other data types
var valb = null;
