
// typescript language supports diffrent types of values 
// it provides data types for javascript to transform into strongly type language

// the type system checks the validate of the given values before the program 
// uses them . It ensures that the code behaves as expected.

// types 

// 1. static    :static types means "at compile time" or "without running the program" : compiler knows them at the compile time 

//     1.1 built in :defined by programming language.
            //number , string , boolean , void  , null 

//     1.2 user-defined :created by programmer.
            //array , touple , enum , class , interface , functions

// 2.generics 

// 3.decorators
